@using TaskManagementApplication.Views.Steps
@model TaskManagementApplication.Views.Steps.DynamicFormViewModel

<form asp-action="FillForm" method="post">
    @Html.HiddenFor(m => m.StepId)

    <div class="modal-header">
        <h5 class="modal-title">Fill Task Form</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
    </div>

    <div class="modal-body">
        @foreach (var field in Model.Fields)
        {
            <div class="mb-4">
                <label class="form-label">@field.Label</label>

                @switch (field.Type)
                {
                    case FieldType.Int:
                    case FieldType.Short:
                    case FieldType.Long:
                        <input type="number" name="@field.Name" value="@field.Value" class="form-control" />
                        break;

                    case FieldType.Decimal:
                        <input type="number" step="0.01" name="@field.Name" value="@field.Value" class="form-control" />
                        break;
           
                    case FieldType.String:
                        <input type="text" name="@field.Name" value="@field.Value" class="form-control" />
                        break;

                    case FieldType.Guid:
                        <input type="text" name="@field.Name" value="@field.Value" class="form-control" />
                        break;

                    case FieldType.Boolean:
                        <div class="form-check">
                            <input type="checkbox" name="@field.Name" class="form-check-input"
                                   @(field.Value is bool b && b ? "checked" : "") />
                            <label class="form-check-label">@field.Label</label>
                        </div>
                        break;

                    case FieldType.DateTime:
                        // Cast the value to DateTime and format it correctly for the input.
                        var dateTimeValue = field.Value is DateTime dt
                        ? dt.ToString("yyyy-MM-ddTHH:mm")
                        : "";
                        <input type="datetime-local" name="@field.Name" value="@dateTimeValue" class="form-control" />
                        break;

                    case FieldType.Dropdown:
                        <select name="@field.Name" class="form-select">
                            @foreach (var option in field.Options ?? new List<string>())
                            {
                                <option value="@option" selected="@(option == (field.Value?.ToString() ?? ""))">@option</option>
                            }
                        </select>
                        break;
                }
            </div>
        }
    </div>

    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <button type="submit" class="btn btn-success">Submit</button>
    </div>
</form>
